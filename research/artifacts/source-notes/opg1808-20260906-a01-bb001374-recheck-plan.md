# 有界复核请求（未执行）

Verdict: `candidate_only`。Primary owner: `math-proof`。

## 输入与冻结

- C01—C05 候选证明及 `research/artifacts/candidates/opg1808-20260906-a01-bb001374-witnesses.json`
- 辅助检查器：`research/artifacts/candidates/opg1808-20260906-a01-bb001374-check-witnesses.py`
- 输入摘要见同包 SHA256 清单。检查器也是候选生成方提供的代码，需先审阅。
- 本地只对检查器作了语法编译，没有运行其中任何图论测试。
- 不把 Python 的可用性改写成仓库通道的 `command_execution=true`。
- 不声称调用过 tournament-enumerator、SAT 或 Lean adapter。

## 固定对象有限复核

在获准的执行环境中，从仓库根目录执行：

```sh
python3 research/artifacts/candidates/opg1808-20260906-a01-bb001374-check-witnesses.py --witnesses research/artifacts/candidates/opg1808-20260906-a01-bb001374-witnesses.json --timeout-seconds 10
```

程序仅检验两个显式对象及 m=1,2,3,4 的构造族，最多7顶点。
每个固定对象枚举其集合分块，并核对定向、三环、强连通及单色可达性。
它不枚举所有三至七顶点着色 tournament，不构成任何无限阶命题的计算证明。
限制：单进程、无网络、最大输入1 MiB、输出64 KiB、墙钟超时10秒（参数最高30秒）。
执行方还应在外部施加内存/CPU限制并记录实际版本、命令、输入摘要、退出码及输出摘要。

## 高保证验证请求

目标义务的 acceptance 需要 kernel_check、axiom_escape_audit、statement_faithfulness。
建议编码 Fin 3/Fin 4 显式弧表、颜色、非平凡分块、统一颜色/方向和至多二色条件。
分块可编码为有限集合上的标签映射，但必须另证该编码覆盖全部分块，不仅是某个分块搜索子集。
为 C03 编码带参数的模块论证，而不是把 m≤4 的检查误作任意 m 的证明。
为 C04、C05 核对非空域、严格变小的归纳对象和每一条路径的颜色。

生成方不签发这些验证器的回执。现有 registry 的 fixture policy 不自动表明其已适用于本问题；
必须由受信任流程确认适用范围。

## 预期而非观察

冻结候选断言：所有列出的对象均无彩虹有向三环、均无要求的分块、均有起点 a；
除 transitive3 外还要求强连通且没有无向彩虹三角形。
本文件没有填写“实际 stdout/退出码”，因为数学检查器尚未执行。
