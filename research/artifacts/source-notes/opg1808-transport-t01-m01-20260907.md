# M01 transport reconciliation: execution assertions remain unverified

Verdict: `candidate_only`. Research Issue: #3. This is a transport note, not a mathematical execution receipt.

The three legacy M01 files were read at branch head `3f885aace987c8ed5437da69aac49a725e3c71b9`. Their UTF-8 bytes were locally hashed after matching the Git blob identities returned by GitHub. They are preserved without changes. The current packet base is `5dadef3eab80ef20ef61c97e93629f9d63c4f4f7`; the base printed inside the old proof describes its original provenance, not the new PR baseline.

## Missing supporting material

The legacy proof refers to an execution report with per-word arrays, local append logs and a separate M01 colour/interval note. These bytes are not present in the three-file branch diff, the current main candidate/source-note trees, or the available chat attachment. The report and separate note have no recoverable exact filename in the supplied material. The driver identifies generated `words-9.json`, `words-10.json`, `runs-9.jsonl` and `runs-10.jsonl`; no such original files were recovered here.

The numerical table, compiler/version assertions, timing comparisons and control assertions in the old proof are historical generator assertions only: **pending/unverified, not replayed by this audit**. The missing records have not been reconstructed, invented or replaced with simulated output. The available source code is not proof that it executed. No missing output digest is asserted.

## Recoverable files and SHA-256

- `research/artifacts/candidates/opg1808-root-m01-materialization-20260906.md`: `b5b2718c1969c718e657bab0956061273f3462c3b841ef46f5d08fc0884ee369`.
- `research/artifacts/candidates/root-path-materialization-m01-20260906.cpp`: `80a85fe495b1e553423eeec98cf9afb0c86709cd8d68f17fde6a5118c417c4f7`.
- `research/artifacts/candidates/root-word-driver-m01-20260906.py`: `e7918d6b089a425685e363ee3967dbb18bb2cae15f2ca1dc5540a921a135e44c`.

## Transport scope and next action

The original M01 and later R03 implementations have different bytes and methods. The M01 branch is therefore preserved, not silently treated as merged or discarded merely because both prose files discuss order ten. Synchronization adds the latest main as a merge parent on the existing branch; no force update or protected-path edit is authorized.

Recover the original missing report/logs/note from their generator source, or commission a separately authorized, version-bound replay. Until then the coverage claims remain unverified. Both admitted obligations remain open; `best_verified_candidate=none` and `best_verified_result=none`.
