# T03: attachment preservation and current transport interpretation

Verdict: `candidate_only`. Issue: #3. This reconciliation introduces no new mathematics and performs no mathematical replay.

## Current transport interpretation takes precedence over historical metadata

This note applies to every imported file whose name starts `opg1808-20260906-a01-bb001374-`. These are historical candidate/source/coordination artifacts, not current permission reports. Their old statements that writes were unavailable, that a branch was merely planned, or that they were not yet submitted describe the earlier preparation episode ONLY. The present GitHub write interface is working; M01 and R04 have already landed via PRs #11 and #12. Neither old pending-smoke labels nor old pre-admission errors are current blockers.

The present transaction starts from fresh main `29ae56c3f1b6903f0ec241a93996cd858f570fef`, uses `web/attempt-opg1808-transport-t03-attachment-20260907`, and reuses Issue #3. Its active packet is `research/artifacts/web-inbox/opg1808-20260906-a01-bb001374-small-order-followup.packet.json`. The real PR binding must be filled before final-head checks. Old prose targets/base revisions record provenance; the present packet binds the already admitted root without editing either obligation or the historical DAG.

## Exhaustive available-attachment disposition

The uploaded ZIP has SHA-256 `b8ce1a55ac24b48fee9ce2447b0ee6bb2507bdcdbc739c0eb92286a8256da2f9` and contains 21 UTF-8 files. Its old manifest and the separately supplied checksum text were checked against the actual available bytes. The separately supplied C06 and C09 files duplicate the corresponding ZIP members and are not submitted twice.

Twenty original file bodies are preserved exactly. The original C01 body explicitly acknowledged duplication of already merged C01; its proof body is not submitted again. A small compatibility pointer at that old locator directs the fixed-witness input to the existing canonical C01. The alternate C02/C03 colourings are different byte-level fixtures from remote C02/C03; their preservation does not claim a new discovery. The other arguments retain their complete written case analysis and attribution, without calling them newly verified or novel.

The original packet body is archived outside the active inbox as `research/artifacts/source-notes/opg1808-20260906-a01-bb001374-small-order-followup.packet.original.json`. It is historical data, not a second active packet. Its old path/hash references and the old manifest refer to the original ZIP layout. The companion T03 disposition manifest states the actual new path/hash mapping. The active inbox path contains a single newly bound packet, not the stale original. Historical Issue/PR drafts are stored as documentary files, not posted as real old operations.

## Execution and source boundaries

All imported proofs, constraints and finite certificates remain candidates. The witness checker and seven-step resolution certificate have NOT been executed here. The original local structure-check JSON records a historical preparation observation, not a newly issued verifier receipt. This audit only handles UTF-8 bytes, hashes, privacy/path/size checks and transport. No theorem prover, solver, graph enumeration or repository mathematical command was run.

The C01-C03 partition obstructions do not negate the root. The failed direction is not retried and no failed-route ledger is edited. Both admitted obligations stay open; `best_verified_candidate=none` and `best_verified_result=none`.

## Reconciliation still requiring original sources

M01's original report, `words-9.json`, `words-10.json`, `runs-9.jsonl`, `runs-10.jsonl`, and its separate colour/interval note were not recovered. R04's original compact report, no-stars logs at orders 3-11, earlier star-strengthened order-eleven logs, timeout/experimental outputs, physical-control data and unpublished uniform colour-run note were not recovered. Exact unnamed source filenames and their hashes remain unknown; none is fabricated. The T01 and T02 transport notes preserve the precise source descriptions and execution caveats.

Completing this PR accounts for all recoverable uploaded file bodies, not for those unavailable execution sources. The final live Issue checkpoint will distinguish zero pending available-file transactions from unresolved original-source recovery. No statement that the whole research archive is fully current is authorized while those source gaps remain.
